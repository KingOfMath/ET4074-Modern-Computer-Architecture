
#ifndef _BENCHMARKS_H_
#define _BENCHMARKS_H_

// Runs the convolution_3x3 benchmark.
int main_convolution_3x3(void);

// Runs the engine benchmark.
int main_engine(void);

// Runs the fir benchmark.
int main_fir(void);

// Runs the adpcm benchmark.
int main_adpcm(void);

#endif
